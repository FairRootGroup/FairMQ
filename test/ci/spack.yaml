spack:
  specs:
    - zlib-ng

  config:
    install_tree:
      root: /opt/spack
      padded_length: 128

  mirrors:
    ghcr-buildcache:
      url: oci://ghcr.io/fairrootgroup/fairmq-spack-buildcache
      signed: false
